name: Build and Deploy Production

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:

jobs:
  build-prod:
    uses: ProductDock/rbc-github-workflows/.github/workflows/build-prod.yml@main
    with:
      update-script: /home/pd-library/update-user-profiles-image.sh
      docker-database-build-arg: true
    secrets: inherit