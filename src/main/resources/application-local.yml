security:
  jwt:
    user-profile:
      private.key: classpath:app-local.key
      public.key: classpath:app-local.pub
      known-issuer: library.productdock.rs
    google:
      jwk-set-uri: https://www.googleapis.com/oauth2/v3/certs
      known-issuer: https://accounts.google.com
