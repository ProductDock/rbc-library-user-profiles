spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://placeholder
          jwk-set-uri: http://placeholder
  mongodb:
    embedded:
      version: 3.5.5

mongock:
  enabled: false

security:
  jwt:
    token:
      secret-key: pdlib